2605: if (this.type === 'striker' && (Math.abs(this.vx) > 0.5 || Math.abs(this.vy) > 0.5)) { if (this.effect !== 'none') createStrikerTrail(this.x, this.y, this.effect); }
2937: function createCrackers(x, y, color, isMega = false) { let count = isMega ? 150 : 80; let speed = isMega ? 25 : 15; for (let i = 0; i < count; i++) particles.push({ x: x, y: y, vx: (Math.random() - 0.5) * speed, vy: (Math.random() - 0.5) * speed, life: 80 + Math.random() * 60, maxLife: 140, color: color, size: (Math.random() * 4 + 2) * (isMega ? 1.5 : 1) }); }
2938: function createPocketEffect(x, y, baseColor) { let activeEffect = pocketEffects.find(e => e.id === equippedPocketEffectId); let colorsToUse = activeEffect ? activeEffect.colors : ['#FFD700', '#00FFFF', '#FF1493', '#39FF14']; let particleCount = activeEffect ? activeEffect.count : 40; for (let i = 0; i < particleCount; i++) { let pColor = Math.random() < 0.3 ? baseColor : colorsToUse[Math.floor(Math.random() * colorsToUse.length)]; particles.push(new Particle(x, y, pColor)); } }
2940: function createStrikerTrail(x, y, effect) {
2952: function drawUpdateParticles() { for (let i = particles.length - 1; i >= 0; i--) { let p = particles[i]; if (p.life) { p.x += p.vx; p.y += p.vy; if (p.color !== '#000000') p.vy += 0.15; p.life--; ctx.globalAlpha = Math.max(0, p.life / p.maxLife); ctx.fillStyle = p.color; ctx.beginPath(); ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2); ctx.fill(); ctx.globalAlpha = 1.0; if (p.life <= 0) particles.splice(i, 1); } else { p.update(); p.draw(); if (p.alpha <= 0) particles.splice(i, 1); } } }